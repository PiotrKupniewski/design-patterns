package identifier;

import model.Product;

import java.util.List;

public class DBProductIdentifier implements ProductIdentififer {
  @Override
  public List<Product> identifyProducts(List<Product> parsedProducts) {
    return null;
  }
}
